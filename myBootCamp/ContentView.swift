import SwiftUI

struct ContentView: View {
    var body: some View {
        
        Spacer()
        
        VStack(spacing:0){
            Spacer()
            RoundedRectangle(cornerRadius: 25)
            Spacer()
            RoundedRectangle(cornerRadius: 25)
            Spacer()
        }
        
        VStack(spacing:0){
            Spacer()
            RoundedRectangle(cornerRadius: 25)
            Spacer()
            RoundedRectangle(cornerRadius: 25)
            Spacer()
        }
        
        Spacer()
    }
}

#Preview {
    ContentView()
}
